package game_code;

public enum RoomTemplate {

	START_ROOM();
	
	RoomTemplate() {
		// TODO: take in inputs for the rooms that the room can be next to along with values such as size and appearance
	}
	
}
